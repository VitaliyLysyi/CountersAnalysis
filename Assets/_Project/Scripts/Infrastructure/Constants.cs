using UnityEngine;

namespace CountersAnalysis
{
    public static class Constants
    {
        public readonly static string DEFAULT_REGISTER_FILE_PATH = Application.persistentDataPath + "/RegisterData.xml";
        public static readonly string REGISTRED_PACKAGES_DIRECTORY = Application.persistentDataPath + "/CountersPackages";
    }
}